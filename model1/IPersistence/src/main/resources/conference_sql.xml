<mapper nameSpace="conference">

    <select id="selectList" resultType="com.lagou.model.Conference"
            paramterType="com.lagou.model.Conference">
        SELECT * FROM `conference`
    </select>

    <select id="selectById" resultType="com.lagou.model.Conference"
            paramterType="com.lagou.model.Conference">
        SELECT * FROM `conference` WHERE id = #{id}
    </select>
</mapper>